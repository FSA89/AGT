<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}AGT Dashboard{% endblock %}</title>

    {# --- СТИЛИ (CSS) --- #}
    {# Bootstrap 5 #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    {# Tabulator (Стиль Bootstrap 5) #}
    <link href="https://unpkg.com/tabulator-tables/dist/css/tabulator_bootstrap5.min.css" rel="stylesheet">
    {# Иконки (опционально, для красоты кнопок) #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

     <link href="/assets/styles/app.css" rel="stylesheet"/>

    <style>
        body {
            padding-top: 70px; /* Отступ, чтобы меню не перекрывало контент */
            background-color: #f8f9fa;
        }
        .tabulator {
            font-size: 14px; /* Чуть компактнее шрифт в таблицах */
        }
        /* Подсветка активной ссылки в меню */
        .navbar-nav .nav-link.active {
            font-weight: bold;
            color: #fff !important;
            text-decoration: underline;
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>

    {# --- ВЕРХНЕЕ МЕНЮ --- #}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">🚀 AGT Admin</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    
                    {# 1. ЗАДАЧИ #}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.get('_route') == 'dashboard_tasks' %}active{% endif %}" 
                           href="{{ path('dashboard_tasks') }}">
                           <i class="fas fa-tasks"></i> Задачи (Tasks)
                        </a>
                    </li>

                    {# 2. СТАТЬИ #}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.get('_route') == 'dashboard_articles' %}active{% endif %}" 
                        href="{{ path('dashboard_articles') }}">
                            <i class="fas fa-file-alt"></i> Статьи
                        </a>
                    </li>

                    {# 3. САЙТЫ #}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.get('_route') == 'dashboard_sites' %}active{% endif %}" 
                        href="{{ path('dashboard_sites') }}">
                            <i class="fas fa-globe"></i> Сайты (ЯВМ)
                        </a>
                    </li>

                    {# 4. ВЫПАДАЮЩЕЕ МЕНЮ: СПРАВОЧНИКИ #}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-book"></i> Справочники
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('dictionary_hreflang') }}">Hreflang</a></li>
                            <li><a class="dropdown-item" href="{{ path('dictionary_page_type') }}">Типы страниц (PageType)</a></li>
                            <li><a class="dropdown-item" href="{{ path('dictionary_neuro') }}">Нейросети (Neuro)</a></li>
                            <li><a class="dropdown-item" href="{{ path('dictionary_scheme') }}">Схемы Нейро (Scheme)</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('dictionary_template') }}">Шаблоны (Templates)</a></li>
                            <li><a class="dropdown-item" href="{{ path('dictionary_cloudflare') }}">Cloudflare Аккаунты</a></li>
                                
                        </ul>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" 
                        href="{{ path('app_generate') }}">
                            <i class="fas fa-globe"></i> Генерировать
                        </a>
                    </li>
                </ul>
                
                {# Правая часть: Юзер и Выход #}
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                         <span class="nav-link disabled text-light">
                            {{ app.user ? app.user.email : 'Гость' }}
                         </span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-danger" href="{{ path('app_logout') }}">Выйти</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    {# --- ОСНОВНОЙ КОНТЕНТ --- #}
    <main class="container-fluid">
        {% block body %}{% endblock %}
    </main>

    {# --- СКРИПТЫ (JS) --- #}
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script type="text/javascript" src="https://unpkg.com/tabulator-tables/dist/js/tabulator.min.js"></script>
    
    {# Блок для JS кода со страниц #}
    {% block javascripts %}{% endblock %}
</body>
</html>